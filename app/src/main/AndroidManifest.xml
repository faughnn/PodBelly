<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".PodbellApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Podbelly"
        android:usesCleartextTraffic="true">

        <!-- Serves generated share-card images to the Android share sheet. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|uiMode"
            android:theme="@style/Theme.Podbelly">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Subscribe deep links. Tapping a podcast://, pcast://, feed:// or
                 itpc:// link (from a share, browser or another app) opens PodBelly
                 and subscribes to the show. The scheme prefixes an https RSS URL;
                 MainActivity reconstructs it via "https:" + schemeSpecificPart. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="podcast" />
                <data android:scheme="pcast" />
                <data android:scheme="feed" />
                <data android:scheme="itpc" />
            </intent-filter>
        </activity>

        <!-- PlaybackService is declared in core:playback's manifest and merged automatically -->

        <!-- Android Auto: declares this app as a media app to the car system. -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

        <!-- Home-screen playback widget. State updates arrive as explicit
             broadcasts from PlaybackService (ACTION_WIDGET_STATE). -->
        <receiver
            android:name=".widget.PlaybackWidgetProvider"
            android:exported="false"
            android:label="Podbelly playback">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_playback_info" />
        </receiver>

        <!-- Chromecast: media3's default provider targets the default media receiver
             (the same receiver app the media3 cast demo uses). -->
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="androidx.media3.cast.DefaultCastOptionsProvider" />

        <!--
            WorkManager's SystemForegroundService must declare foregroundServiceType
            so that DownloadWorker can run as a DATA_SYNC foreground service on API 34+.
            Without this the OS throws a MissingForegroundServiceTypeException.
        -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <!--
            Disable the default WorkManagerInitializer so that our custom
            Configuration.Provider (PodbellApp) with HiltWorkerFactory is used.
            Without this, WorkManager initialises before Hilt and DownloadWorker
            injection silently fails.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>
</manifest>
